.nh
.TH "LAGO" "1" "Sep 2026" "Lago CLI" "Lago CLI Manual"

.SH NAME
lago-quotes-approve-quote-version - Approve a quote version


.SH SYNOPSIS
\fBlago quotes approve-quote-version  [flags]\fP


.SH DESCRIPTION
This endpoint approves a quote version and generates the order form to send to the customer for signature.
Only a \fBdraft\fR version can be approved, and its \fBbilling_items\fR must be complete: approval validates them against the catalog and against the customer, and returns a \fB422\fR when the deal cannot be executed as quoted.
The \fBexpires_at\fR of the generated order form is bounded by the deal's own term, so a signing window reaching beyond the day the deal stops being executable is rejected.
A concurrent write on the same quote is reported as a \fB422\fR rather than retried, so a duplicated call can fail while the first one is still in flight.
This is a premium feature.


.SH OPTIONS
\fB--expires-at\fP=""
	The date and time in UTC (ISO 8601) when the order form generated by the approval expires. It must be in the future, and must fall strictly before the day the quoted deal stops being executable, otherwise the approval is rejected with a '422'. That boundary is the earliest of the 'endDate' of any quoted plan and the 'expirationAt' of any quoted wallet credit or recurring top-up rule. A 'one_off' quote carries none of them, so its signing window is never bounded. When 'expires_at' is omitted, the order form never expires.

.PP
\fB-h\fP, \fB--help\fP[=false]
	help for approve-quote-version

.PP
\fB--input\fP=""
	Complete JSON request body or @file.json


.SH OPTIONS INHERITED FROM PARENT COMMANDS
\fB--api-key\fP=""
	Override the Lago API key

.PP
\fB--api-url\fP=""
	Override the Lago API URL

.PP
\fB--confirm\fP=""
	Confirm a dangerous operation with its resource identifier

.PP
\fB--dry-run\fP[=false]
	Print mutating requests without sending them

.PP
\fB--insecure\fP[=false]
	Allow insecure HTTP or TLS for self-hosted Lago

.PP
\fB--mode\fP=""
	Environment mode: live or test

.PP
\fB--no-retry\fP[=false]
	Disable automatic retries

.PP
\fB-o\fP, \fB--output\fP="table"
	Output format: table, json, or yaml

.PP
\fB--profile\fP=""
	Named profile to use

.PP
\fB--query\fP=""
	JMESPath expression applied to the response

.PP
\fB--timeout\fP=30s
	Total request timeout

.PP
\fB--timing\fP[=false]
	Print request latency breakdown

.PP
\fB--verbose\fP[=false]
	Print redacted request and response details


.SH EXAMPLE
.EX
  lago quotes approve-quote-version <lago_id> --input @payload.json
  lago quotes approve-quote-version <lago_id> --input @payload.json --output json  # full resource
.EE


.SH SEE ALSO
\fBlago-quotes(1)\fP
